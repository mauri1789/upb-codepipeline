version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7

    commands:
      - ./build.sh -i

  pre_build:
    commands:
      - echo build


  build:
    commands:
      - ./build.sh -b

  post_build:
    commands:
      - echo post_builds

artifacts:
  files:
    - template.yaml
  name: upbCodepipeline
  
